# PushQuiz Client (React + Vite)
- `npm i`
- `npm run dev` (dev server)
- `npm test` (Vitest)
- `npm run build` (production build)

Connects via WebSocket to `ws://localhost:8080/v1/rooms/{roomId}/play`.
